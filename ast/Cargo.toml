[package]
name = "raft-ast"
version = "0.1.0"
edition.workspace = true

[features]
std = ["raft-lexer/std"]
default = ["std"]

[dependencies]
raft-lexer = { path = "../lexer", default-features = false }
